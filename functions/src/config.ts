export const PROJECT_ID = 'calorix-xurschnell';
export const LOCATION = 'us-central1';
export const APP_DISPLAY_NAME = 'AppName';

// Stage 2 keeps the historical hardcoded model; Stage 4 replaces this with the
// server-owned model_configs document so retirements never require a deploy.
export const VISION_MODEL = 'gemini-2.5-flash';
